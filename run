apt update
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
apt -y install nodejs
ufw allow 7107
ufw allow 443
ufw allow 80
ufw allow 8008
git clone https://github.com/superade99/prox.git && cd prox
cp .env.example .env
npm install
sudo node server.js
